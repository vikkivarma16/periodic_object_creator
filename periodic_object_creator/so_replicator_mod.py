# periodic_object_creator/so_replicator

def replicator(input_object):
    object_return = []
    for i in range(len(input_objct)):
        object_return.append(input_object[i])
    return object_return

